name: code_excerpter
publish_to: none

environment:
  sdk: ^3.1.0

dependencies:
  build: ^2.4.1
  logging: ^1.2.0
  path: ^1.8.3
  yaml: ^3.1.2

dev_dependencies:
  analysis_defaults:
    path: ../analysis_defaults
  build_runner: ^2.4.6
  test: ^1.24.8
